# Codex Handoff - 2026-02-26 07:59

## Completed
- Implemented Quirk Museum backend flow in CLI:
  - `museum --in --out`
  - `suggest-labels --in --out`
  - `apply-label --specimen --label --labels`
- Added deterministic specimen IDs using SHA256 of canonical JSON.
- Added static museum dashboard output (`data.json` + `index.html`).
- Added rule-based heuristic label suggestions and manual curation command.
- Added Vite dashboard at `tools/quirk-museum-vite` with:
  - clickable glass timeline scroller
  - soft orange-yellow quirk bubbles
  - color coding by quirk class
  - specimen detail panel with artifact links
- Added docs:
  - `docs/museum.md` updates for Vite usage
  - `tools/quirk-museum-vite/README.md`

## Validation run
- `cargo check -p jurassic-bitcoin-cli`
- `cargo test -p jurassic-bitcoin-cli`
- `cargo run -p jurassic-bitcoin-cli -- museum --in artifacts/era-2009-2013 --out artifacts/museum`
- `cargo run -p jurassic-bitcoin-cli -- suggest-labels --in artifacts/era-2009-2013 --out artifacts/museum/suggestions.json`
- `cargo run -p jurassic-bitcoin-cli -- apply-label --specimen <id> --label STACK_UNDERFLOW_STRUCTURAL --labels artifacts/museum/labels.json`
- Vite app: `npm install`, `npm run sync:data`, `npm run build`

## Notes
- Added `.gitignore` entries for `tools/quirk-museum-vite/node_modules`, `dist`, and `public/data.json`.
- Local environment policy blocked cleanup commands, but generated frontend artifacts are now ignored.
